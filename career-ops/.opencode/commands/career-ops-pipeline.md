---
description: Legacy alias for score discovery queue
---

Run the legacy `pipeline` alias, which now maps to `score discovery` behavior.
This scores queued discovery jobs from data/pipeline.md without report/PDF generation.

Load the career-ops skill:
```
skill({ name: "career-ops" })
```
