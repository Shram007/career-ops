---
description: Scan job portals via WebSearch discovery queries only
---

Scan job portals using WebSearch discovery queries (Level 3 only) with career-ops scan discovery mode.

Load the career-ops skill:
```
skill({ name: "career-ops" })
```
